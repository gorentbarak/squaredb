dict = { lcbracket  ~ ( kv ~ "," )* ~ rcbracket }

kv       =  { key ~ space* ~ ":" ~ space* ~ value }
space    = _{ " " }
cbracket =  { rcbracket  | lcbracket }
	rcbracket = { "}" }
	lcbracket = { "{" }
